<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_greenpedia"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_greenpedia"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dicodingpemula"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Dicodingpemula">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- About Activity -->
        <activity
            android:name=".AboutActivity"
            android:label="About"
            android:exported="true">
        </activity>

        <!-- Detail Activity -->
        <activity
            android:name=".DetailActivity"
            android:label="Detail"
            android:exported="true">
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!--        &lt;!&ndash; Add Article Activity &ndash;&gt;-->
<!--        <activity-->
<!--            android:name=".AddArticleActivity"-->
<!--            android:label="Add Article"-->
<!--            android:exported="true">-->
<!--        </activity>-->

    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>